apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fury-api.fullname" . }}-test-connection"
  labels:
    {{- include "fury-api.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fury-api.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
