apiVersion: v2
name: digital-me-api
description: Digital Me API - Backend for AI-assisted writing platform

type: application
version: 0.1.0
appVersion: "1.0"

# Add PostgreSQL as a dependency
dependencies:
  - name: postgresql
    version: 15.0.0   # Update to the desired PostgreSQL version
    repository: https://charts.bitnami.com/bitnami
    condition: postgresql.enabled
